def main():
    # un-pickle bugs? Take bugs from previous step?
    # Loop over all bugs
    #   If bugs are not resolve or not > week long or < 5 comments continue
    #   Create snapshot
    #   If < 5 comments continue
    #   Save it to filtered collection
    return